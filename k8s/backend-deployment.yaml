apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: training-bank-rks # Add this line
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
      # No namespace needed here, it's inherited from the Deployment's metadata
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: backend
          image: ghcr.io/fullera8/trainingbank-backend
          ports:
            - containerPort: 8080
          # Uncomment and configure the following if you need environment variables (e.g., for DB connection) change
          # env:
          #   - name: ConnectionStrings__DefaultConnection
          #     valueFrom:
          #       secretKeyRef:
          #         name: sql-connection-secret
          #         key: connection-string